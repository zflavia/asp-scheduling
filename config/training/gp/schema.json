{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "GP Training Configuration Schema",
  "type": "object",
  "required": ["algorithm", "instances_file", "seed", "environment"],
  "properties": {
    "algorithm": {
      "type": "string",
      "enum": ["gp", "gp-aos", "gp-dr"],
      "description": "The algorithm to use — only 'gp' and 'gp-aos' is valid for this template."
    },
    "instances_file": {
      "type": "string",
      "description": "Path to the file with generated training data."
    },
    "saved_model_name": {
      "type": "string",
      "description": "Optional filename to save the finished model."
    },
    "experiment_save_path": {
      "type": "string",
      "description": "Model output directory."
    },
    "seed": {
      "type": "array",
      "description": "Seed for all pseudo-random generators (random, numpy, torch).",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "integer",
        "minimum": 0
      }
    },
    "no_parallel_processes": {
      "type": "integer",
      "description": "Number of parallel processes used by DEAP library."
    },
    "environment": {
      "type": "string",
      "description": "The environment to use (e.g., 'env_gp')."
    },
    "gp_population_size": {
      "type": "integer",
      "minimum": 10,
      "description": "Number of individuals in the population."
    },
    "gp_halloffame_size": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of elite individuals preserved across generations."
    },
    "gp_tree_initial_max_depth": {
      "type": "integer",
      "minimum": 2,
      "description": "Maximum depth allowed for a tree in GP initialization step."
    },
    "gp_tree_max_depth": {
      "type": "integer",
      "minimum": 5,
      "description": "Maximum depth allowed for a tree in GP."
    },
    "gp_generations_number": {
      "type": "integer",
      "minimum": 10,
      "description": "Stooping criteria: Number of repetitions."
    },
    "gp_simplify_frequency": {
      "type": "integer",
      "minimum": 1,
      "description": "The population is simplified at each k iterations."
    },
    "gp_population_variation": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "How often a variation operator (3 mutation types, 2 crossover types)  is selected [0,1]."
    },
    "gp_aos_type": {
      "type": "string",
      "enum": ["aos", "epsilon-qlearning", "random"],
      "description": "The GP operator selection variant."
    },
    "gp_tournament_size": {
      "type": "integer",
      "minimum": 2,
      "description": "Tournament selection size"
    },

    "total_instances": {
      "type": "integer",
      "minimum": 0,
      "description": "Not used, kept for compatibility with other agents."
    },
    "total_timesteps": {
      "type": "integer",
      "minimum": 0,
      "description": "Not used, kept for compatibility with other agents."
    }
  }
}
